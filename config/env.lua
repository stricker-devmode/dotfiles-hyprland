-- ENVIRONMENT VARIABLES
-- define as key=value pairs where value is always a string
return {
    -- cursor
	XCURSOR_THEME = "Future-cursors",
	XCURSOR_SIZE = "25",
	HYPRCURSOR_SIZE = "25",
	-- Set Qt platform theme, qt5ct implies qt6ct for Qt6 apps
	QT_QPA_PLATFORMTHEME = "qt5ct",
}
